### Imports ###
from os.path import dirname, join

### Config file ###
configfile: join('config', 'config.yaml')

### Setup ###
global_resources = workflow.global_resources

USE_GPU = ('gpus' in global_resources) and (global_resources['gpus'] > 0)

### Config variables ###
input_dir = config['input_dir']
output_dir = config['output_dir']
script_dir = config['script_dir']
template_dir = config['template_dir']
outlier_ids = config['outlier_ids']
if outlier_ids is None:
    outlier_ids = []

### Input and output variables ###
include: join('rules', 'variables.smk')

### Targets ###
# The order is: column, groupA, groupB
# If you want to constrain a different column to a value (subset the samples),
# you can specify the column as 'column1=value__column2'
TARGET_COMBINATIONS = [
    join('{0}', '{1}_{2}').format(*combs) for combs in
    [
        ['segment', 'stroma', 'tumor'],
        ['segment=stroma__NACT_status', 'pre', 'post'],
        ['segment=tumor__NACT_status', 'pre', 'post'],
    ]
]
TARGET_DOTPLOTS = expand(
    join(output_dir, 'gsea', '{combination}', '{modality}', '{test}',
        '{metric}', '{geneset_name}', 'dotplot.png'),
    combination=TARGET_COMBINATIONS,
    modality=config['modalities'],
    test=config['tests'],
    metric=config['metrics'],
    geneset_name=config['gene_sets'],
)
TARGET_VOLCANO_PLOTS = expand(
    join(output_dir, 'volcano', '{combination}', '{modality}', '{test}',
        '{metric}', '0.05', 'volcano_plot.png'),
    combination=TARGET_COMBINATIONS,
    modality=config['modalities'],
    test=config['tests'],
    metric=config['metrics'],
)
# The order is: combination to be used for pairing, column, groupA, groupB
# If you want to constrain a different column to a value (subset the samples),
# you can specify the column as 'column1=value__column2'
LIMMA_COMBINATIONS = [
    join('{0}', '{1}', '{2}_{3}').format(*combs) for combs in
    [
        ['patient+main_batch_nr+ROI_index_tCycIF',
            'segment', 'stroma', 'tumor'],
        ['patient', 'segment=stroma__NACT_status', 'pre', 'post'],
        ['patient', 'segment=tumor__NACT_status', 'pre', 'post'],
    ]
]
TARGET_LIMMA = expand(
    join(output_dir, 'limma', '{combination}', '{modality}', 'results.tsv'),
    combination=LIMMA_COMBINATIONS,
    modality=config['modalities'],
)
TARGET_LIMMA_GSEA = expand(
    join(output_dir, 'limma', '{combination}', '{modality}', 'gsea',
        '{geneset_name}', 'dotplot.png'),
    combination=LIMMA_COMBINATIONS,
    modality=config['modalities'],
    geneset_name=config['gene_sets'],
)

### Rules ###
rule all:
    input:
        TARGET_LIMMA,
        TARGET_LIMMA_GSEA,
        TARGET_DOTPLOTS,
        TARGET_VOLCANO_PLOTS,
        INDEGREE_FILE,
    default_target:
        True

include: join('rules', 'preprocessing.smk')
include: join('rules', 'networks.smk')
include: join('rules', 'comparison.smk')
include: join('rules', 'gsea.smk')
include: join('rules', 'volcano.smk')
include: join('rules', 'limma.smk')